# Lineamientos de Contribucion

Toda solicitud de extraccion (Pull Request) orientada a modificar el motor (archivos dentro de `engine/` o `core/`) debe estar acompanada de sus respectivas pruebas unitarias en el directorio `tests/`.

El codigo debe adherirse a los estandares de PEP 8.
